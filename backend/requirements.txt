# ===== P1 骨架阶段依赖 =====
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
alembic>=1.13.0
# greenlet：锁定 <3.2 以使用 Python 3.9 的预编译 wheel，避免本地编译需要 MSVC
greenlet<3.2
pydantic>=2.6.0
pydantic-settings>=2.1.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
python-multipart>=0.0.9
Pillow>=10.0.0
httpx>=0.27.0           # 高德地理编码调用
rembg>=1.0.50           # AI 抠图（首次使用会下载 ~170MB u2net 模型）
onnxruntime>=1.16.0     # rembg 2.x 不再强制依赖，需显式声明

# ===== 后续待启用 =====
# opencv-python-headless  # 印章增强（如需 CLAHE/形态学高级处理时启用）
